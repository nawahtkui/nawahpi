import { HORIZON_URL } from '../../.env';

export async function swapTokens(amount, fromAsset, toAsset, wallet) {
    console.log(`Swapping ${amount} ${fromAsset} to ${toAsset} for wallet ${wallet}`);
}
